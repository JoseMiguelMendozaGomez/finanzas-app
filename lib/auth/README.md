# lib/auth

Helpers y configuración de la capa de autenticación.

## Contenido previsto

- `session.ts` — utilidades para leer y verificar la sesión del usuario
- `config.ts` — configuración del proveedor de autenticación
- `middleware.ts` — helpers reutilizables para proteger rutas
